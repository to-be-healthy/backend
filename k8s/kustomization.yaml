apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tobehealthy

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml

# Image tag updated automatically by CI (.github/workflows/deploy.yml).
# Do not edit by hand; use `kustomize edit set image`.
images:
  - name: ghcr.io/to-be-healthy/backend
    newName: ghcr.io/to-be-healthy/backend
    newTag: sha-e59c86623365
