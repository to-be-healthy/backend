spring:
  profiles:
    active: DEV

  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace

jwt:
  secret: YP/7oEXp2DRFZYxUm7eYBbIlBMpApIhPcYGtDStbtDI=
  access-token-valid-seconds: 7200    # 2 hour
  refresh-token-valid-seconds: 604800 # 7 day

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  tomcat:
    uri-encoding: UTF-8

file:
  upload:
    location: /Users/seonwoo_jung/upload # 로컬 실행시 변경

# Spring openapi
springdoc:
  packages-to-scan: kr.szs.pjt.presentation
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: /
    disable-swagger-default-url: true
    display-request-duration: true
    operations-sorter: alpha